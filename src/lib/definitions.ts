// src/lib/definitions.ts
export type User = {
    
    displayName: string,
    mail: string,
    jobTitle:string,
    officeLocation: string,
    newAccessToken: string,
    refresh_token: string,
};